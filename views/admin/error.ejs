<!doctype html>
<html lang="vi">
<head><%- include('../partials/head', { pageTitle: title }) %></head>
<body>
  <div class="admin-auth-page" style="background:var(--bg-page)">
    <div class="auth-card" style="text-align:left">
      <h1 style="text-align:center"><%= title %></h1>
      <p style="color:var(--text-muted); font-size:0.88rem; line-height:1.6"><%= message %></p>
      <p style="text-align:center; margin-top:18px">
        <a class="btn btn-outline" href="/admin">Về trang chủ</a>
      </p>
    </div>
  </div>
</body>
</html>
